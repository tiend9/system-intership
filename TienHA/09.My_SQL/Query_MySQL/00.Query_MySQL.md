# TÌM HIỂU VỀ CÁCH TRUY VẤN DATABASES (QUERY)
SELECT – Các truy vấn cơ bản

SELECT

SELECT FROM

SELECT DISTINCT

Table & Column Aliases

WHERE

AND

OR

IN

NOT IN

BETWEEN

LIKE

IS NULL

LIMIT

ORDER BY

📁 2. Joins – Kết hợp nhiều bảng

Joins (tổng quan)

INNER JOIN

LEFT JOIN

RIGHT JOIN

CROSS JOIN

Self Join

📁 3. Aggregation – Nhóm & tổng hợp dữ liệu

GROUP BY

HAVING

HAVING COUNT

ROLLUP

📁 4. Subqueries – Truy vấn con

Subquery

Derived Tables

EXISTS

📁 5. Set Operations – Toán tử tập hợp

EXCEPT

INTERSECT

📁 6. (Nếu cần cho đủ 31 mục – tách rõ ràng JOIN)

JOIN Overview (đã có nhưng bạn có thể tách làm mục riêng)

Aggregate Functions (COUNT, SUM, AVG…) – nên thêm vì liên quan HAVING COUNT, GROUP BY

Nếu bạn muốn giữ đúng 31 mục không thêm gì, thì mục 30 & 31 vẫn là 2 phần tách ra từ nhóm JOIN và GROUP BY, không tạo lệnh mới.
- Là câu lệnh MySQL để lấy, lọc, thêm, sửa, xóa dữ liệu trong database.

## I. QUERYING DATA

### 1. SELECT`





## II. INSERT & UPDATE & DELETE DATA

## II. MANAGING DATABASE

## III. MANAGING TABLES

## IV. MYSQL CONSTRAINTS

## V. MYSQL IMPORT & EXPORT

## VI. MYSQL GOBALIZATION

